const API_PREFIX = 'api/v1/';

const API = {
  MAIN: 'https://february-21.herokuapp.com/',
  LOGIN: 'api-token-auth/',
  QUOTES: `${API_PREFIX}quotes/`,
  SUPPLIERS: `${API_PREFIX}suppliers/`,
  USERS: `${API_PREFIX}users/`
};

export default API;
